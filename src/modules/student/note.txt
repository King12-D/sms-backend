 const newStudent = new studentmodel({
      age,
      studentClass,
      studentid: studentId,
    });
    const created = await newStudent.save();
    if (created) {
      res.status(201).json({
        success: true,
        message: "Student Created  Successfully",
      });
    } else {
      res.status(400).json({
        success: true,
        message: "Unable to create  student  please try again",
      });
    }










    for (let i = 0; i < maxAttempts; i++) {
        studentId = Math.floor(10000 + Math.random() * 900000);
        const exists = await studentmodel.exists({studentid:studentId});
        if(!exists){
            break;
        }
        
     }